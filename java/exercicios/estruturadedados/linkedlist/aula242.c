// lista -> lista encadeada/ lista simplesmente encadeada -> sequência de elementos encadeados, um após o outro.
// diff de lista, fila e pilha -> 
// pilha -> insere no topo e remove sempre no topo
// lista ->  pode remover/inserir no inicio, meio, fim... Pode fazer buscas...
// fila -> insere no final e remoção no inicio

//  OBS
// sempre que usar lista, crie um ponteiro auxiliar para o primeiro nó da nossa lista, para sempre manter a referência para o 
// primeiro nó da nossa lista.
// excluir nó -> criar um ponteiro auxiliar pra esse nó "noRemoverSetaProximo", que apontará pro nó depois desse nó que será
// removido, assim dando essa apontada para o nó que tava antes do nó que foi removido

// lista duplamente encadeada -> tem seta pra ir e pra voltar ou seja cada nó possui 2 ponteiros
// lista circular -> o ultimo nó aponta para o primeiro nó


